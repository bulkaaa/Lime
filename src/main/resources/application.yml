# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection
# push command: git push heroku LimeAppHeroku:master
# heroku config:add DATABASE_URL="mysql://user:password@host/db?reconnect=true&createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8"
# heroku credentials:ó
#   username:limemini102@gmail.com
#   password:LIMEmini102!@
# gmail password: limeminipassword
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming.strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  session:
    store-type: redis
server:
  session:
    timeout: 43200
storage:
  properties:
    location: pics