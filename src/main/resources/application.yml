# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection
# push command: git push heroku LimeAppHeroku:master
# heroku credentials:ó
#   username:limemini102@gmail.com
#   password:LIMEmini102!@
# gmail password: limeminipassword
spring:
  datasource:
    url: jdbc:mysql://b5d13ead58f709:99f604c2@us-cdbr-iron-east-05.cleardb.net/heroku_126b7c0bd7511a7?reconnect=true&createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming.strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  session:
    store-type: redis
server:
  session:
    timeout: 25920000
storage:
  properties:
    location: pics